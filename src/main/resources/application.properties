# spring.application.name=ltdd
# spring.datasource.url=jdbc:mysql://localhost:3306/ltdd
# spring.datasource.username=root
# spring.datasource.password=

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# server.port=8080


server.address=0.0.0.0
server.port=${PORT:8080}

spring.application.name=backend

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- CẤU HÌNH GMAIL GỐC (Port 587) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Điền đúng Email và Mật khẩu ứng dụng của nó
spring.mail.username=nguyenhuunghia03052004@gmail.com
spring.mail.password=nhhipzoneukhqyct
# (Thay xxxx bằng mã 16 ký tự của bạn)

spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Tin tưởng chứng chỉ Google
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# Debug & Timeout
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.connectiontimeout=10000
spring.mail.properties.mail.timeout=10000